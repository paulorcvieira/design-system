import { Meta, StoryObj } from '@storybook/react'
import { Widget } from './'

export default {
  title: 'Widget UI/Widget Component',
  component: Widget,
} as Meta

export const Default: StoryObj = {}